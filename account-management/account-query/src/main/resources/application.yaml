server:
  port: 8082
  error:
    include-message: always

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: user

security:
  oauth2:
    resource:
      jwt:
        key-value: bankappsigningkey